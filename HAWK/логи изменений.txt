k1-fw-alfa-by-fagci-20260719_0419
 - Сейчас в качестве источника для предфильтра в сканировании пробуем AGC RSSI вместо обычного RSSI — по наблюдениям точнее садится на канал, меньше "перелёта" на соседний.
 - Дебаг-меню анализатора (долгий 0)
   Добавлены регистры радиотракта для экспериментов со скоростью реакции:
   PLL/VCO включая тот самый импульс калибровки, полоса ПЧ-фильтра, входные каскады усиления (LNA/MIX/PGA), выбор ПЧ, DC-фильтр приёма, AFC, пороги и скорость AGC.

   Выбор параметра на графике анализатора:
   Клавиша F открывает оверлей, короткий 0 в нём листает, что рисовать на графике:
   RSSI, Noise, Glitch, LNA peak RSSI, AGC RSSI, upper/lower power, SNR, signal power, AFC (по модулю отклонения). 1/7 двигают измерительную полку с числом.
   Масштаб графика теперь автоматически подстраивается под реальный диапазон каждого параметра — раньше многие из них выглядели плоской линией из-за фиксированной шкалы.


k1-fw-alfa-by-fagci-20260717_1134
 - WFM (радиовещательный FM) больше не теряет настройку полосы при её ручном изменении
 - Ручная настройка усиления (gain/AGC) больше не сбрасывается на авто после одного нажатия PTT
 - Добавлен переключатель AVC (авто-громкость) в debug-меню анализатора (долгое 0)
 - Сканер запоминает, где остановился, и продолжает оттуда при повторном входе — не с начала диапазона
 - Командный режим сканирования (был реально сломан, теперь работает)
 - Профиль из 2+ команд наконец-то реально проходит все команды, а не зацикливается на первой
 - Частота на экране больше не "зависает" — видно реальный прогресс сканирования
 - Нумерация команд теперь по-человечески (1/2, а не 0/2)
 - Частота VFO больше не перетирается после выхода из командного скана
 - Тип команды, приоритет, авто-whitelist переключаются кнопками */F, как везде
 - Поля ввода (частота, диапазон, задержка) теперь пустые при редактировании — не надо стирать старое значение
 - SIDE1/SIDE2 (чёрный/белый список) теперь корректно продолжают скан, как в обычном Scanner
 - Добавлено перемещение команд вверх/вниз (SIDE1/SIDE2 в списке команд)
 - Починен баг с созданием тестового файла команд (не грузился из-за неверного расширения)
 - Убран нерабочий код (мёртвые переменные/функции), исправлен баг с принудительной перерисовкой экрана, который тихо не срабатывал


k1-fw-alfa-by-fagci-20260717_0746
 - Сканер стал быстрее и стабильнее: не залипает на одном уровне шумодава, не подвисает на паузах, точнее ловит сигнал (не проскакивает частоту из-за AFC)
 - PTT из сканера в VFO теперь мгновенный — раньше была заметная задержка
 - Частота VFO больше не "теряется" после сканирования - после выхода из скана радио возвращается на ту частоту, что было до него, и это сохраняется даже после выключения
 - Список найденного (Loot) показывает правильное время обнаружения каждой станции, а не одно и то же для всех
 - Задержка замера в сканере теперь запоминается, не сбрасывается при каждом открытии
 - Исправлен баг: после захода в список найденного и выхода сканер не залипал в режиме графика VFO
 - Убран нерабочий пункт VOX из меню назначения кнопок (чтобы не сбивал с толку)
 - В debug-меню анализатора (долгое 0) добавлен переключатель компандера
 - На экране VFO во время передачи показывается уровень входного аудио


k1-fw-alfa-by-fagci-20260715_0657
 - Скан по методу Карины с ожиданием 4мс, Задержку можно уменьшать до 1мс
